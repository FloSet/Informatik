/*imperative program*/void main() {
    int steps = 0;
	while(vornFrei()){
        vor();
        steps++;
    }
    linksDrehen(steps);
}

void linksDrehen (int count){
    while(count>0){
    	linksUm();
    	count--;
    }
}